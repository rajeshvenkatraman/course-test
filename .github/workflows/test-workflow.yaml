name: Ubuntu Commands.
on: [pull_request]
jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a string
        run: echo "Hello World"
      - name: MultiLine Command
        run: |
          node -v
          npm -v
